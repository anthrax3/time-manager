// Simple React Native specific changes

export const apiConfig = {
  host: 'https://timesheet-staging-aurity.herokuapp.com',
  baseUrl: '/api',
  users: '/users',
  training: '/training/weeks/'
};

export default {
  // font scaling override - RN default is on
  allowTextFontScaling: true
}
